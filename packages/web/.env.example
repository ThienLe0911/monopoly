# WebSocket URL of the room server. Leave unset for local dev (defaults to ws://localhost:8080).
# In Vercel, set this as a Production Environment Variable to the deployed Render server, e.g.:
# VITE_WS_URL=wss://monopoly-server.onrender.com
VITE_WS_URL=
